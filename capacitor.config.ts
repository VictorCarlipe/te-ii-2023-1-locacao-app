import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'te-ii-2023-1-locacao-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
